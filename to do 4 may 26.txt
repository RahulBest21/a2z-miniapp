═══════════════════════════════════════════════════════════════
  A2Z UPDATES — COMPREHENSIVE IMPLEMENTATION PLAN
  Last updated: 05 May 2026 | Status: PHASE 0 DONE, PHASE 1 IN PROGRESS
═══════════════════════════════════════════════════════════════

╔══════════════════════════════════════════════════════════════╗
║  🆕 NEW REQUIREMENTS (05-May-2026) from Rahul + UI Doc      ║
╚══════════════════════════════════════════════════════════════╝

  REQ-A | Gemini Rephrase → Auto-Tag Every Question
      └─ Jab Gemini rephrase kare, har question ko subject-wise,
         chapter-wise, topic-wise, sub-topic-wise tags assign kare.
      └─ Tags store honge in: questions table (tag columns) + new
         taxonomy tables (subjects, chapters, topics, subtopics).
      └─ Gemini prompt update: add "Also return subject, chapter,
         topic, subtopic, difficulty_level, cognitive_level tags."

  REQ-B | Per-User Topic-Level Analytics Storage
      └─ Store karna h ki usne kis particular topic/subtopic me
         kitne questions attempt kiye, kitne correct, kitne wrong,
         kitne skip kiye.
      └─ Isse user ko har subtopic/topic/chapter/subject ki
         ACCURACY and ATTEMPT RATE milegi.
      └─ New aggregation tables: user_topic_stats, user_subject_stats,
         user_chapter_stats (updated on every mock submit).

  REQ-C | Score History Per Mock Group (Series)
      └─ Har mock group/ series ka score history track karna h.
      └─ User dekhe: "SP Mock Series me 4/10 mocks attempted,
         avg score 35/50, best 42/50, trend improving."
      └─ New table: user_mock_group_stats.

  REQ-D | AI-Pretending Analytics (Deterministic + Gemini Polish)
      └─ Sab computation local/deterministic hoga (pure SQL + Python).
      └─ But user ko dikhega "AI Analysis" label ke saath, jaise
         premium AI tool ne analysis kiya ho.
      └─ Gemini sirf "copywriter" ki tarah use hoga: analytics JSON
         dekar Hinglish me 3-4 line personalized feedback generate.
      └─ Fallback: template-based insights agar Gemini quota khatam.

  REQ-E | Bilingual Hindi + English Explanations
      └─ Har question ka explanation bilingual hona chahiye
         (Hindi + English dono me).
      └─ Gemini rephrase ke time hi both languages ka explanation
         generate karega.
      └─ Current schema already has: explanation (EN) + explanation_hi.

  REQ-F | "Premium AI" Feel — Har Aspect Pe Best Advice
      └─ Dashboard ya Advanced Analytics me har respect pe best
         data-driven advice dikhe.
      └─ Weak areas, strong areas, time management tips, silly
         mistake detection, stamina analysis, etc.
      └─ Sab data-centric + AI-polished presentation.

  REQ-G | Website Login (Alongside Telegram Auth)
      └─ Telegram Mini App ke saath-saath ek web login bhi hoga.
      └─ Username unique hoga. Data JSON me store hoga.
      └─ Separate web dashboard for deeper analytics view.
      └─ Detailed spec in PHASE 5A and PHASE 10.

⬛ LEGEND
  [✓] Done     [~] In Progress     [ ] Pending     [x] Blocked
  HIGH = must fix before deployment
  MED  = core feature needed for launch
  LOW  = nice-to-have, post-launch
  P0-P4 = Priority tier (0 = critical, 4 = future)

═══════════════════════════════════════════════════════════════
PHASE 0 — CRITICAL BUG FIXES  (P0)
═══════════════════════════════════════════════════════════════

[✓] BUG-01 | mainmock.py     | Delete orphaned code block at lines 1967-2016
      └─ Removed floating file-handler block referencing undefined
         `update`, `status`, `context` at module level.
      └─ Fix: ~50 lines deleted. (Done: 2026-05-04)

[✓] BUG-02 | mainmock.py     | Add db_execute, db_commit, db_fetchone, db_fetchall
      └─ Import at line 33 was missing these 4 essential DB helpers
         which are used throughout the bot but never imported.
      └─ Fix: Added to `from a2z_db import (...)` at line 34.
         (Done: 2026-05-04)

[✓] BUG-03 | a2z_miniapp.html| Remove extra `}` at line 396
      └─ Duplicate closing brace after showLeaderboard() function
         caused JS parse error.
      └─ Fix: Deleted the orphaned `}`. (Done: 2026-05-04)

[✓] BUG-04 | a2z_miniapp.html| Delete orphaned code at lines 479-483
      └─ Floating shareApp body (let text... tg.openTelegramLink...)
         with unmatched `}` outside any function scope.
      └─ Fix: 5 lines deleted. Real shareApp() is at line 488.
         (Done: 2026-05-04)


═══════════════════════════════════════════════════════════════
PHASE 1 — CORE INTEGRATION (Mini App ↔ Bot)  (P0-P1)
═══════════════════════════════════════════════════════════════

[✓] FEAT-05 | a2z_miniapp.html + mainmock.py | completeQuest() → Bot
      └─ WAS: XP awarded client-side only, never persisted to DB.
      └─ NOW: sendToBot('complete_quest', {xp, streak, rank}) added.
              Bot _webapp_data handler calls complete_daily_quest()
              from a2z_gamification.py. Client still updates UI
              immediately for responsiveness.
      └─ STATUS: Code written. Needs end-to-end test.
      └─ Files: a2z_miniapp.html:548-560, mainmock.py:2301-2313

[✓] FEAT-06 | a2z_miniapp.html + mainmock.py | Dynamic Mock Browser
      └─ WAS: MOCK_CATEGORIES hardcoded with 4 exams, 8 mock entries.
      └─ NOW:
           • DEFAULT_MOCK_CATEGORIES retained as fallback
           • Added getMockCategories() → checks window.dynamicMockCategories
             first, falls back to DEFAULT_MOCK_CATEGORIES
           • Added updateMockCache(data) to parse bot's mock-list JSON
             into the grouped-by-exam→subjects structure
           • renderMockBrowser() now uses getMockCategories()
           • Bot's get_mocks handler queries DB, builds mock_list
      └─ STATUS: Mini-app side done. Bot side needs structured-data
             reply (see FEAT-07). Currently bot only sends plain text.
      └─ Files: a2z_miniapp.html:217-309, mainmock.py:2198-2214

[✓] FEAT-07 | mainmock.py + a2z_miniapp.html | Structured Data Pipeline
      └─ PROBLEM: _webapp_data builds state_json and mock_list as Python
         dicts but only sends plain text via reply_text(). Mini app
         cannot read the reply (sendData is one-way, closes app on mobile).
      └─ SOLUTION A (Recommended): Store state_json + mock_list JSON
         in users.state_json column. Mini app sends a second
         sendToBot('get_updates') call. Bot reads state_json, replies
         with JSON as text. Mini app can parse it from the reply
         (works on desktop/web where sendData doesn't close).
      └─ FOR NOW (Phase 1): Implement Solution A.
         Steps:
         1. Modify get_state handler to serialize state + mock_list
            to JSON, store in users.state_json
         2. Add get_updates action that reads state_json and replies
            with the stored JSON
         3. Mini app: after sendToBot('get_state'), call
            sendToBot('get_updates') and parse the response
         4. Wire parseBotResponse() → updateMockCache(), updateUI()
      └─ SUB-TASKS:
         [✓] 7a. Wrote store_state(user_id, json_data) + get_stored_state()
                in a2z_db.py (lines ~256-270). Added state_json column to
                users table schema + ALTER TABLE migration.
         [✓] 7b. Modified get_state handler to build state_data + mock_list
                + guild_info, store combined JSON via store_state().
                (mainmock.py:2284-2326)
         [✓] 7c. Added get_updates action to _webapp_data — reads
                get_stored_state(), sends readable summary to chat.
                (mainmock.py:2328-2351)
         [✓] 7d. Added fetchUpdates() + syncAll() functions in mini app.
                Added 🔄 Sync button in header. (a2z_miniapp.html:473-487)
         [ ] 7e. Test end-to-end: mini app → bot → mini app roundtrip
      └─ STATUS: Code written (2026-05-05). Needs end-to-end test.
      └─ FILES: a2z_db.py:41,256-270, a2z_miniapp.html:473-487,
                mainmock.py:2284-2351

[✓] FEAT-08 | a2z_miniapp.html | Leaderboard in Mini App
      └─ Current state: showLeaderboard(mockId) sends
         sendToBot('get_leaderboard', {mock_id}) but never
         populates lb-entries with actual data.
      └─ Bot's get_leaderboard handler already works — returns ranked
         list from get_leaderboard() DB function.
      └─ SUB-TASKS:
         [✓] 8a. Bot stores leaderboard in state_json. Leaderboard preserved
                across get_state calls. (mainmock.py:2247-2285)
         [✓] 8b. Render ranked list in #lb-entries with medals 🥇🥈🥉.
                renderLeaderboardEntries() added. (a2z_miniapp.html:406-460)
         [✓] 8c. Show user's own rank highlighted with "YOU" badge.
         [✓] 8d. Added "Refresh" button on leaderboard.
      └─ STATUS: Code written (2026-05-05). UI renders from state.lbData.
         Data flows: showLeaderboard → bot stores LB → syncAll preserves it.
         LIMITATION: Mini app can't parse bot reply directly (one-way sendData).
         LB visible after next sync/reload. Full fix needs HTTP endpoint (INFRA).
      └─ Files: a2z_miniapp.html:395-460, mainmock.py:2247-2285

[✓] FEAT-09 | a2z_miniapp.html + mainmock.py | Guild UI
      └─ Current state: renderGuild() uses state.guild which is never
         populated. Guild create/join sendToBot works but UI never
         updates with guild info.
      └─ SUB-TASKS:
         [✓] 9a. Added get_guild action to bot's _webapp_data
                → reads user's guild from DB, returns guild info + members.
                Also added guild_leave action. (mainmock.py:2303-2345)
         [✓] 9b. get_state handler already includes guild_info via
                get_guild_info(). Data stored in state_json and preserved
                across sessions.
         [✓] 9c. Updated renderGuild() to show member list with XP, quest
                status, multiplier, guild ID. Added fetchGuild() refresh.
                (a2z_miniapp.html:475-543)
         [✓] 9d. Added leave guild button with confirmation popup.
                leaveGuildAction() → sendToBot('guild_leave').
      └─ STATUS: Code written (2026-05-05). Guild tab fetches on switch.
         Same limitation as FEAT-08: data flows through state_json storage.

[✓] FEAT-10 | a2z_miniapp.html | Profile "My Stats" Button
      └─ requestMyStats() function exists (line 458) but no UI
         button triggers it. Bot handler already generates Hunter
         License HTML and sends as document.
      └─ Task: Add a clickable button in Profile tab that calls
         requestMyStats(). Show loading state until bot reply arrives.

[ ] FEAT-11 | a2z_miniapp.html + mainmock.py | Website Login System
      └─ REQ-G: Separate web login alongside Telegram auth.
      └─ Username unique, data stored in JSON.
      └─ Web dashboard for deeper analytics (later phase).
      └─ SUB-TASKS:
         [ ] 11a. Add login page (standalone HTML or mini-app tab)
         [ ] 11b. Username/password + JWT token auth
         [ ] 11c. Link Telegram user_id to web account
         [ ] 11d. Cross-platform session sync (Telegram ↔ Web)
      └─ PRIORITY: LOW — core features first, web login after.


═══════════════════════════════════════════════════════════════
PHASE 2 — ADMIN DASHBOARD (Bot Side)  (P1)
═══════════════════════════════════════════════════════════════

Key requirement from Rahul:
  "kitne referral h, kitne paid h, kaun-kya manage karna h,
   kitne total users h, full secure, 1 mock me kitne attempts hue"

[✓] ADMIN-01 | mainmock.py | /admin command — Full Dashboard
      └─ Current: /dashboard exists but returns basic text stats.
      └─ Required: Rich admin panel with sub-commands:
         [✓] /admin overview   → Total users, active users, mocks_count,
                                 total_attempts, invites_generated,
                                 paid_users (placeholder)
         [✓] /admin users      → List users with ID, name, XP, rank,
                                 invites, access status.
         [✓] /admin mocks      → List all mocks with ID, title, q_count,
                                 attempt_count, avg_score, avg_accuracy
         [✓] /admin user <id>  → Deep view: all attempts, invite chain,
                                 access history, invited-by info.
         [✓] /admin invites    → Invite leaderboard: who invited whom,
                                 verification status.
         [✓] /admin broadcast  → Send message to all active users.
      └─ FILES: mainmock.py:1974-2195 (handler + 8 sub-functions)

[✓] ADMIN-02 | mainmock.py | Mock registry via /admin mocks
      └─ Lists all registered mocks with: mock_id, title, section,
         question_count, attempts, avg_score, avg_accuracy.

[✓] ADMIN-03 | mainmock.py | Admin-only security
      └─ is_admin() check on /admin command.
      └─ /admin add <user_id> to promote admins.
      └─ log_admin_action() writes to admin_log table.

[✓] ADMIN-04 | a2z_db.py | Admin query functions
      └─ get_admin_overview()
      └─ get_admin_mocks_list() → get_admin_mocks_list()
      └─ get_user_deep_stats(user_id)
      └─ get_admin_invite_network()
      └─ get_admin_all_user_ids()
      └─ log_admin_action()
      └─ FILES: a2z_db.py:657-769

[✓] MIGR-01 | Live DB | Applied gamification schema migration
      └─ ALTER TABLE users: added xp, rank, streak_days, last_active_date,
         guild_id, legacy_tag, state_json columns.
      └─ Created tables: xp_log, daily_quests, guilds, guild_members,
         raid_events, raid_damage, hidden_quests.
      └─ Done: 2026-05-05


═══════════════════════════════════════════════════════════════
PHASE 3 — GROWTH ECOSYSTEM (Referral + PvP + Gating)  (P1-P2)
═══════════════════════════════════════════════════════════════

The "Free Mock → Gated Premium Analysis" funnel:
  1. Free mock = top-of-funnel hook
  2. After test = desire for rank + analysis (Sunk Cost Fallacy)
  3. To unlock analysis: invite friends OR pay
  4. Tiers: 3 invites → 15 days | 5 invites → 30 days | Paid → 30 days

[✓] GROWTH-01 | a2z_db.py + mainmock.py | Referral Verification
      └─ WAS: invite counted on link-click / channel-join.
      └─ NOW: "Active-Only" — referral verified when invited user
         completes an action (submits a mock, does daily quest).
      └─ SUB-TASKS:
         [✓] 1a. Added 'action_verified' column to invites table (bool, default 0).
         [✓] 1b. On user completes action → verify_referral_by_action() checks
                if they were invited → marks invite as action_verified →
                awards XP to inviter via award_xp().
         [✓] 1c. Bot sends message to inviter: "🎯 Referral verified!
                +50 XP. Your friend just completed their first mock/quest."
      └─ FILES: a2z_db.py:120,375-394, mainmock.py:1747,2353
      └─ Done: 2026-05-05

[✓] GROWTH-02 | mainmock.py + a2z_db.py | Access Tiers
      └─ Implement:
         Tier 1: 3 verified invites = 15 days premium access
         Tier 2: 5 verified invites = 30 days premium access
         Tier 3: Paid = 30 days (placeholder, integrate UPI later)
      └─ SUB-TASKS:
         [✓] 2a. Added premium_access_expiry + premium_tier columns to
                users table. has_free_access() checks both free and premium.
         [✓] 2b. grant_premium_access() tracks tier and expiry.
         [✓] 2c. /access command → user sees current tier, invites used,
                days remaining, action-verified count.
         [✓] 2d. check_and_grant_access() updated: 5 verified = Tier 2 (30d),
                3 verified = Tier 1 (15d), 1 verified = 7d trial.
      └─ FILES: a2z_db.py:37-38,260-309,401-429, mainmock.py:1987-2020
      └─ Done: 2026-05-05

[✓] GROWTH-03 | mainmock.py + a2z_db.py | 1v1 PvP Challenge Engine
      └─ challenges table, create/accept/complete functions.
      └─ /challenge <mock_id> → generates deep link.
      └─ Rival opens link → auto-accepts → both take mock → bot compares.
      └─ Scorecard: Head-to-Head with winner (+100 XP).
      └─ Auto-detects pending challenges on mock submit.
      └─ "You scored 42/50. Generate Challenge Link to dare a rival."
      └─ SUB-TASKS:
         [ ] 3a. After mock submit, bot offers /challenge command
         [ ] 3b. Generates deep-link: t.me/A2ZUpdatesBot?start=challenge_<match_id>
         [ ] 3c. Rival clicks → takes same mock → bot compares scores
         [ ] 3d. Generate "Head-to-Head Scorecard" text/image:
                User A: 42/50 (84%)  vs  User B: 38/50 (76%)
                Winner: User A (+100 XP)
         [ ] 3e. Challenge_completed action → awards XP, unlocks analysis

[✓] GROWTH-04 | mainmock.py + a2z_db.py | AIR 209 Benchmark System
      └─ benchmark_score column on mocks table.
      └─ /admin set_benchmark <mock_id> <score>.
      └─ On submit: score >= benchmark → instant 30-day premium unlock.
      └─ "BENCHMARK CRUSHED!" message with celebration.

[✓] GAME-01 | mainmock.py | Raid Boss System
      └─ /raid → check active raid status.
      └─ /raid create <name> <hp> <reward> (admin).
      └─ Auto-deal 1 damage on: quest complete, mock submit.
      └─ Uses existing create_raid + deal_raid_damage from a2z_gamification.

[✓] GAME-02 | mainmock.py | Rank Decay Auto-Check
      └─ Already wired in _start: apply_rank_decay() on every login.
      └─ Loses 50 XP per 5 inactive days. Revokes access if below threshold.

[✓] GAME-03 | mainmock.py | Daily Quest Auto-Send
      └─ Already wired in _start: create_daily_quest() on every login.
      └─ New quest auto-generated each day for each user.

[✓] GAME-04 | mainmock.py | Per-Question Answer Tracking
      └─ Already wired: save_response() called in submit flow.
      └─ Per-question responses stored in responses table.
      └─ Analytics functions use this data for weak-area detection.


═══════════════════════════════════════════════════════════════
PHASE 7 — DEPLOYMENT  (P0 — Do after Phase 1)
═══════════════════════════════════════════════════════════════

[✓] DEPLOY-01 | Git | Push to GitHub
      └─ git init → add → commit → push -u origin main
      └─ Repo: https://github.com/RahulBest21/a2z-miniapp
      └─ Commit: "Phase 0-3 + admin + growth + API" — 29 files, 40,947 LOC
      └─ Done: 2026-05-05

[✓] DEPLOY-02 | GitHub | Enable GitHub Pages
      └─ Settings → Pages → Source: main branch → Save
      └─ LIVE: https://rahulbest21.github.io/a2z-miniapp/a2z_miniapp.html

[✓] DEPLOY-03 | BotFather | Set Menu Button
      └─ @BotFather → @A2ZUpdatesBot → Menu Button
      └─ URL: https://rahulbest21.github.io/a2z-miniapp/a2z_miniapp.html
      └─ Text: "📝 Take Mock" — loaded & working

[✓] DEPLOY-04 | Verify End-to-End
      └─ Bot opens → menu button → mini app loads ✓


═══════════════════════════════════════════════════════════════
PHASE 8 — INFRASTRUCTURE  (P2-P3)
═══════════════════════════════════════════════════════════════

[✓] INFRA-01 | mainmock.py | Background Scheduler
      └─ asyncio task runs every 5 min.
      └─ Daily quest auto-send (6 AM IST) ✓
      └─ Rank decay check (midnight IST) ✓
      └─ Premium access expiry check (hourly) ✓

[✓] INFRA-02 | mainmock.py | Data Export/Backup
      └─ /admin backup — JSON dump of all tables.
      └─ /admin export_users — CSV of all users.
      └─ /admin export_mocks — CSV of all mocks.

[ ] INFRA-03 | Cloudflare Tunnel Setup (for API + Mini App HTTPS access)
      └─ Bot API runs on localhost:8765. Cloudflare Tunnel exposes it publicly.
      └─ Steps:
         [ ] 3a. Install: winget install cloudflare.cloudflared
         [ ] 3b. Run: cloudflared tunnel --url http://localhost:8765
         [ ] 3c. Copy the *.trycloudflare.com URL
         [ ] 3d. Set API_BASE_URL in a2z_miniapp.html to that URL

[✓] INFRA-04 | a2z_miniapp.html | Pull-to-Refresh
      └─ Swipe-down gesture reloads state from bot/API.
      └─ 🔄 Sync button also available in header.
      └─ Solves bot→mini-app two-way communication (sendData is one-way).
      └─ aiohttp server runs alongside bot on port 8765.
      └─ ENDPOINTS:
         [✓] GET /api/health → health check
         [✓] GET /api/state?token=<auth_token> → full stored state JSON
         [✓] GET /api/leaderboard?token=<auth_token>&mock_id=<id> → leaderboard
         [✓] GET /api/analytics?token=<auth_token> → user stats + recent attempts
      └─ MINI APP:
         [✓] syncFromAPI() → fetches state, merges into UI
         [✓] syncLeaderboardFromAPI(mockId) → fetches leaderboard
         [✓] Configurable API_BASE_URL (set to Cloudflare Tunnel URL)
         [✓] Auth via auth_token (from URL param or CloudStorage)
      └─ BOT: get_auth_token action returns user's API token.
      └─ Requires Cloudflare Tunnel (INFRA-03) for public HTTPS access.
      └─ FILES: a2z_api.py (new, 160 lines), mainmock.py, a2z_miniapp.html
      └─ Done: 2026-05-05

[ ] INFRA-03 | Cloudflare Tunnel Setup (for API + Mini App HTTPS access)
      └─ Bot API runs on localhost:8765. Cloudflare Tunnel exposes it publicly.
      └─ Steps:
         [ ] 3a. Install: winget install cloudflare.cloudflared
         [ ] 3b. Run: cloudflared tunnel --url http://localhost:8765
         [ ] 3c. Copy the *.trycloudflare.com URL
         [ ] 3d. Set API_BASE_URL in a2z_miniapp.html to that URL
         [ ] 3e. For permanent domain: cloudflared tunnel create + route DNS
      └─ Add pull-to-refresh gesture or "🔄 Sync Data" button
      └─ Calls fetchState() → sendToBot + re-render all tabs


═══════════════════════════════════════════════════════════════
PHASE 9 — MONETIZATION & ANALYTICS  (P3-P4)
═══════════════════════════════════════════════════════════════

[✓] MON-01 | mainmock.py | Paid Access Integration
      └─ /admin pay <user_id> <days> — grant paid premium access.
      └─ grant_premium_access() with tier="paid".

[ ] MON-02 | Analytics Dashboard (Mini App)
      └─ Admin tab in mini app showing:
         - Real-time user count, active today
         - Mock attempts per day (chart)
         - Referral network graph
         - Revenue tracking

[ ] MON-03 | A/B Testing Framework
      └─ Test different invite thresholds (3 vs 5)
      └─ Track conversion rates per test group


═══════════════════════════════════════════════════════════════
QUICK SUMMARY — REMAINING ITEMS
═══════════════════════════════════════════════════════════════

  1. [ ] FEAT-11 — Website Login System (LOW priority)
  2. [ ] CONTENT-01/02 — Editorial upload + daily quest XP
  3. [ ] INFRA-03 — Cloudflare Tunnel setup
  4. [ ] MON-02 — Admin analytics dashboard in mini app
  5. [ ] MON-03 — A/B testing framework (future)
  6. [ ] PHASE 4 remaining — editorial quiz auto-generate

═══════════════════════════════════════════════════════════════
